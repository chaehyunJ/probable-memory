1) 박스 배치 부터
- 눈에 보이게 하려고 테두리나 배경색을 적용

2) 내부 요소 HTML만 작성
- 디테일은 배치가 끝난 이후에 작성해도 늦지 않다

3) 요소의 정렬을 어떻게 수행할지?
- block 으로 세로 쌓기
- flex 로 가로 배치
- flex(column) 으로 세로 배치 (정렬)
- inline, inline-block 으로 가로배치
- 다수의 아이템을 박스안에서 정렬하려면 wrap

4) 박스의 배치와 독립적으로 배치할 요소가 있으면
position 속성으로 자리 잡아주기

Ϙ
